export interface IXp
{
  formula: string,
  channel: string,
  embed: {
    title: string,
    description: string,
    color: number,
    timestamp: boolean,
    thumbnail: string
    image: string
  }
}
